# Frontend configuration for Replit
run = "python -m http.server 8000"
modules = ["python-3.10", "web"]

[deployment]
run = ["sh", "-c", "python -m http.server 8000"]
deploymentTarget = "web"
publicDir = "."

[nix]
channel = "stable-21.11"

[custom]
deployment.userSelection = "frontend"
frontend = "/frontend"